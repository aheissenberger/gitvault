---
id: "S-20260301-R060"
title: "REQ-60 platform-specific security features"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli"]
scope: { repoAreas: ["src/**", "tests/**", ".github/workflows/**"], touch: ["src/**", "tests/**", ".github/workflows/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-60 (NFR): platform-specific security controls are used where applicable; for Windows, secret material files use restricted ACL handling (e.g., via `icacls`) instead of Unix permission semantics."
  - id: "AC2"
    text: "Cross-platform behavior is explicit and deterministic: Unix-like systems enforce owner-only mode (0600), while Windows enforces user-restricted ACLs."
verification: { commands: ["cargo test --all"] }
risk: { level: "medium" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-60

## Verification
- OS-specific permission enforcement paths are documented and validated through platform-appropriate tests/CI lanes
