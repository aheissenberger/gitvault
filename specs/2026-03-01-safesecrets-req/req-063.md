---
id: "S-20260302-R063"
title: "REQ-63 classic and hybrid identity compatibility"
status: "draft"
owners: ["@aheissenberger"]
mode: ["cli", "vscode-bg"]
scope: { repoAreas: ["src/**", "README.md", "specs/**"], touch: ["src/identity.rs", "src/fhsm.rs", "src/commands/**", "README.md", "specs/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Identity profile `classic` and `hybrid` are both supported and explicitly labeled in command output/metadata."
  - id: "AC2"
    text: "Selector precedence (`--identity-selector` over `GITVAULT_IDENTITY_SELECTOR`) and fail-closed ambiguity rules apply equally to classic and hybrid identities."
  - id: "AC3"
    text: "CI and `--no-prompt` modes produce deterministic outcomes for zero/multiple usable keys across both profiles."
verification: { commands: ["cargo xtask spec-verify", "cargo test --all-targets --all-features"] }
risk: { level: "high" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-63

## Verification
- Profile compatibility tests for identity resolution and selector behavior
- CI/non-interactive tests for ambiguity and fail-closed semantics
