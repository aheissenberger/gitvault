---
id: "S-20260301-R059"
title: "REQ-59 mock external dependencies with mockall"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli"]
scope: { repoAreas: ["src/**", "tests/**"], touch: ["src/**", "tests/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-59 (NFR): tests use the `mockall` crate to mock external dependencies (for example AWS SDK clients and AWS SSM interactions) instead of requiring live external services."
verification: { commands: ["cargo test --all"] }
risk: { level: "low" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-59

## Verification
- external dependency boundaries are trait-based and can be mocked with `mockall` in unit/integration tests
