---
id: "S-20260301-R030"
title: "REQ-30 redact ssm diff by default"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli"]
scope: { repoAreas: ["src/**"], touch: ["src/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-30: ssm diff does not reveal secret values unless --reveal is explicitly set."
  - id: "AC2"
    text: "Values are displayed as `***` by default and as the real value only when `--reveal` is passed."
verification: { commands: ["cargo test --all"] }
risk: { level: "high" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-30

## Evidence
- `src/ssm.rs` `cmd_ssm_diff`: `let display = if reveal { v.as_str() } else { "***" };` — values masked by default.
- `src/cli.rs` `SsmAction::Diff { reveal: bool }` — `--reveal` flag controls the parameter.
- Consistent with `gitvault decrypt --reveal` convention used elsewhere in the codebase (REQ-6).
