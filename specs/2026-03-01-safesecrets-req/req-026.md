---
id: "S-20260301-R026"
title: "REQ-26 backend policy vault or ssm"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli"]
scope: { repoAreas: ["src/**"], touch: ["src/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-26: env policy defines backend as vault or ssm."
  - id: "AC2"
    text: "The SSM backend is gated behind the `ssm` Cargo feature flag; the `Ssm` CLI subcommand is only compiled and available when the feature is enabled."
verification: { commands: ["cargo test --all", "cargo build --features ssm"] }
risk: { level: "medium" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-26

## Evidence
- `src/cli.rs`: `Ssm { env, action }` subcommand is `#[cfg(feature = "ssm")]`-gated.
- `Cargo.toml`: `ssm = ["dep:aws-config", "dep:aws-sdk-ssm", "dep:tokio"]` feature definition.
- `src/main.rs`: dispatch to `crate::ssm::cmd_ssm_*` is `#[cfg(feature = "ssm")]`-gated.
- All SSM operations are inaccessible without the feature, providing a clean backend-policy mechanism.
