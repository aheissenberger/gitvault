---
id: "S-20260302-R062"
title: "REQ-62 keyring-first identity creation"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli", "vscode-bg"]
scope: { repoAreas: ["src/**", "README.md", "specs/**"], touch: ["src/keyring_store.rs", "src/identity.rs", "src/commands/**", "README.md", "specs/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "`identity create` stores identity material in keyring by default and supports optional file export via `--out <path>`."
  - id: "AC2"
    text: "Optional file export enforces restrictive file permissions and preserves no-prompt/CI safety guarantees."
  - id: "AC3"
    text: "Unavailable/locked keyring is represented as `source-not-available`; identity resolution continues by precedence and fails closed if unresolved."
verification: { commands: ["cargo xtask spec-verify", "cargo test --all-targets --all-features"] }
risk: { level: "high" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-62

## Verification
- Keyring-first create tests with optional `--out` path behavior
- Permission-hardening tests for exported identity files
- Non-interactive fallback tests for locked/unavailable keyring
