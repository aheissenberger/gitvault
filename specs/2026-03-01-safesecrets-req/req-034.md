---
id: "S-20260301-R034"
title: "REQ-34 optional .env merge driver"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli"]
scope: { repoAreas: ["src/**"], touch: ["src/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-34: merge driver is available for .env and can be installed by `gitvault harden` via .gitattributes entry plus local git config `merge.gitvault-env.driver`."
verification:
  commands:
    - "cargo test --manifest-path Cargo.toml test_cmd_harden_registers_merge_driver_git_config"
    - "cargo test --manifest-path Cargo.toml --test cli_integration harden_installs_merge_driver_and_git_merge_uses_it"
risk: { level: "low" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-34

## Verification
- Runtime merge command and algorithm are implemented and wired.
- `gitvault harden` installs merge-driver prerequisites end-to-end:
  - writes `*.env merge=gitvault-env` to `.gitattributes`
  - configures local git config `merge.gitvault-env.driver`.
- Integration test validates real `git merge` execution path through the configured driver.
