---
id: "S-20260301-R048"
title: "REQ-48 non-interactive CI support"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli", "vscode-bg"]
scope: { repoAreas: ["src/**", ".github/workflows/**"], touch: ["src/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-48: fully non-interactive CI usage is supported."
  - id: "AC2"
    text: "CI identity resolution uses deterministic precedence `--identity -> GITVAULT_IDENTITY -> keyring -> ssh-agent (optional)` and fails closed on ambiguous agent keys without prompt."
  - id: "AC3"
    text: "CI selector precedence is deterministic (`--identity-selector` over `GITVAULT_IDENTITY_SELECTOR`), and unavailable keyring/agent sources are treated as source-not-available before final fail-closed evaluation."
verification: { commands: ["cargo test --all"] }
risk: { level: "medium" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-48

## Verification
- CI non-interactive via GITVAULT_IDENTITY env var and --no-prompt / CI auto-detection
- CI non-interactive identity extension in progress for optional keyring passphrase and optional SSH-agent fallback while preserving deterministic behavior.
- CI tests validate selector precedence, source-not-available fallback, and fail-closed unresolved identity behavior.
