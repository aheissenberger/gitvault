---
id: "S-20260301-R046"
title: "REQ-46 no-prompt for all commands"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli"]
scope: { repoAreas: ["src/**"], touch: ["src/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-46: all commands support --no-prompt."
  - id: "AC2"
    text: "In `--no-prompt` mode, identity selection remains deterministic and fails clearly on SSH-agent key ambiguity (no implicit key picking)."
  - id: "AC3"
    text: "In `--no-prompt` mode, `--identity-selector` overrides `GITVAULT_IDENTITY_SELECTOR`; if no identity source resolves successfully, commands fail closed with actionable output."
verification: { commands: ["cargo test --all"] }
risk: { level: "low" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-46

## Verification
- --no-prompt flag on all commands in src/cli.rs Cli struct and resolve_no_prompt
- Non-interactive identity-source resolution validates precedence and ambiguity errors without prompting.
- Non-interactive selector precedence and source-not-available fallback are covered by CLI tests.
