---
id: "S-20260301-R007"
title: "REQ-7 encrypted artifacts under secrets"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli"]
scope: { repoAreas: ["src/**"], touch: ["src/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-7: encrypted artifacts reside under `.gitvault/store/`, including env-scoped and keep-path layouts (`.gitvault/store/<env>/...`). **Breaking change:** was `secrets/`."
verification: { commands: ["cargo test --all"] }
risk: { level: "low" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-7

## Verification
- encrypted artifacts stored under `.gitvault/store/` via src/repo/paths.rs helpers
- `encrypt --keep-path` stores `<repo-relative>/file` as `.gitvault/store/<env>/<repo-relative>/file.age`
