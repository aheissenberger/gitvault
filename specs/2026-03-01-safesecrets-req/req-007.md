---
id: "S-20260301-R007"
title: "REQ-7 encrypted artifacts under secrets"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli"]
scope: { repoAreas: ["src/**"], touch: ["src/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-7: encrypted artifacts reside under secrets/, including env-scoped and keep-path layouts (`secrets/<env>/...`)."
verification: { commands: ["cargo test --all"] }
risk: { level: "low" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-7

## Verification
- encrypted artifacts stored under secrets/ via src/repo/paths.rs helpers
- `encrypt --keep-path` stores `<repo-relative>/file` as `secrets/<env>/<repo-relative>/file.age`
