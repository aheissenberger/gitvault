---
id: "S-20260301-R038"
title: "REQ-38 key rotation"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli"]
scope: { repoAreas: ["src/**"], touch: ["src/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-38: recipient/key rotation is supported. **Breaking change (REQ-72):** the `gitvault rotate` command is removed and superseded by `gitvault rekey`. The new command has identical semantics (re-encrypt all `.age` files to the current recipients) but uses the directory-based recipients model (`.secrets/recipients/*.pub`). Any scripts referencing `gitvault rotate` must be updated to `gitvault rekey`."
verification: { commands: ["cargo test --all"] }
risk: { level: "high" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-38

## Verification
- key rotation previously in `src/commands/recipients.rs cmd_rotate`; superseded by `gitvault rekey` (REQ-72, `src/commands/rekey.rs`) which re-encrypts all `.age` files to the current recipients directory
