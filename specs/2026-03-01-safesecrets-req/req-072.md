---
id: "S-20260303-R072"
title: "REQ-72 recipient ceremony — identity pubkey + rekey"
status: "draft"
owners: ["@aheissenberger"]
mode: ["cli"]
platforms: ["macOS", "Linux", "Windows"]
scope:
  repoAreas: ["src/**", "specs/**", "README.md", "docs/ai/**"]
  touch:
    - "src/cli.rs"
    - "src/dispatch.rs"
    - "src/commands/identity.rs"
    - "src/commands/rekey.rs"
    - "src/commands/mod.rs"
    - "tests/**"
    - "README.md"
    - "docs/ai/skill.md"
    - "specs/**"
  avoid: ["target/**"]
acceptance:
  - id: "AC1"
    text: "`gitvault identity pubkey` is a new subcommand of `gitvault identity`. It prints the age public key of the currently resolved identity (same resolution order as all other commands: `--identity` flag → `GITVAULT_IDENTITY` env var → OS keyring → SSH-agent) to stdout as a single line. Exit code 0 on success, 2 if no identity can be resolved."
  - id: "AC2"
    text: "`gitvault identity pubkey --json` emits a JSON object: `{ \"public_key\": \"age1...\" }`. The plain-text (non-JSON) output is the raw public key string with no label or decoration, so it can be piped directly: `gitvault identity pubkey >> .secrets/recipients/alice.pub`. This is a low-level escape hatch; the preferred path is `recipient add-self` (AC14)."
  - id: "AC3"
    text: "`gitvault rekey` is a new top-level command. It re-encrypts every `.age` file found under the configured secrets directory (default `.secrets/`) to the current recipient set (`.secrets/recipients`). The plaintext of each file is decrypted with the caller's identity and immediately re-encrypted; no plaintext is written to disk."
  - id: "AC4"
    text: "Rekey operates atomically per file: the new `.age` ciphertext is written to a temp file in the same directory, then renamed over the original. If any file fails (decrypt error, recipient error, write error), the error is reported and that file is left unchanged; rekey continues processing remaining files and exits with code 1 after finishing."
  - id: "AC5"
    text: "Rekey only processes files where the recipient set has actually changed since the file was last encrypted. Files whose current recipients already match the recipient set are skipped and reported as `unchanged`. This prevents unnecessary churn in git history."
  - id: "AC6"
    text: "`gitvault rekey --dry-run` prints what would change (which files would be re-encrypted, which skipped) without writing anything. Exit code 0."
  - id: "AC7"
    text: "`gitvault rekey --json` emits a JSON array: one object per processed file with fields `file` (string), `status` (`re-encrypted` | `skipped` | `error`), and `error` (string, only present when status is `error`). The array is emitted after all files are processed."
  - id: "AC8"
    text: "`gitvault rekey --env <name>` restricts processing to `.age` files whose path contains the environment segment (e.g. `--env dev` only rekeyed files under `.secrets/dev/`). Without `--env`, all environments are rekeyed."
  - id: "AC9"
    text: "If the caller's identity cannot decrypt a given file (i.e. they are not a current recipient), that file is reported with status `skipped` (not `error`) and a message noting they lack read access. Rekey exits with code 0 if all failures are of this kind."
  - id: "AC10"
    text: "The canonical team onboarding flow works end-to-end: (1) new member runs `gitvault identity create --add-recipient` to create a key and add their own public key to the recipients directory in one command; (2) they commit and push `.secrets/recipients/<name>.pub` as a PR; (3) after the PR is merged, a maintainer who can decrypt all secrets runs `gitvault rekey` to re-encrypt everything for the updated recipient set; (4) the new member can `git pull` and decrypt normally. This flow requires no private key to ever leave its origin machine."
  - id: "AC11"
    text: "Rekey reports a summary line at the end: `Rekeyed N file(s), M skipped, K error(s)`. In `--json` mode this summary appears as a `summary` key at the top level alongside the `files` array."
  - id: "AC12"
    text: "Both `identity pubkey` and `rekey` respect the global `--config` flag and `GITVAULT_CONFIG` env var for config file resolution."
  - id: "AC13"
    text: "README and `docs/ai/skill.md` are updated to document the recipient ceremony flow (`identity create --add-recipient` → PR → `rekey`), `gitvault identity pubkey`, `gitvault recipient add-self`, and `gitvault rekey` including all flags."
  - id: "AC14"
    text: "`gitvault recipient add-self` is a new `RecipientAction` variant. It resolves the caller's age identity (same source priority as all other commands), derives the public key, and adds it to the recipients directory as a named file `<git-username>.pub` (falling back to `self.pub` if git config `user.name` is unavailable). It validates the public key before writing, is idempotent (no-ops and reports if the key is already present), and exits with code 2 if no identity can be resolved."
  - id: "AC15"
    text: "Recipients are stored as **named per-person files** under a `recipients/` subdirectory: `.secrets/recipients/<name>.pub`. Each file contains exactly one public key. `gitvault recipient list` reads all `.pub` files from the directory and reports each with its filename. `gitvault recipient add <pubkey>` writes to `<git-username>.pub` by default; an optional `--name <label>` flag overrides the filename. Existing tools that read recipients (`encrypt`, `rekey`, `materialize`) are updated to scan the directory instead of a single flat file. Backward compatibility: a legacy single-file `.secrets/recipients` (no extension, no subdirectory) is still accepted and a deprecation notice is printed prompting the user to run `gitvault recipient migrate`."
  - id: "AC16"
    text: "`gitvault recipient migrate` is a new `RecipientAction` variant that converts a legacy flat `.secrets/recipients` file to named per-person files. Each line in the flat file becomes `.secrets/recipients/recipient-N.pub` (N = 1, 2, …). The flat file is removed after migration. `--dry-run` supported. This is a one-time housekeeping command."
  - id: "AC17"
    text: "`gitvault identity create` accepts a new `--add-recipient` flag. When set, after the identity is created (and stored per `--out`/keyring), the command automatically runs the `recipient add-self` logic to add the new public key to `.secrets/recipients/<git-username>.pub`. If no git repository or recipients directory is found the flag is silently ignored and a warning is printed, so the flag is safe to use during first-time machine setup outside any repo."
---

## Context

This spec covers the pieces that complete **Option A — recipient ceremony** (zero-shared-secret team onboarding):

1. **`gitvault identity pubkey`** — print own public key (low-level escape hatch).
2. **`gitvault recipient add-self`** — add own public key to the recipients directory in one command (preferred path).
3. **`gitvault identity create --add-recipient`** — create key and register as recipient in a single step.
4. **Named per-person recipient files** — `.secrets/recipients/<name>.pub` for surgical git offboarding.
5. **`gitvault rekey`** — atomically re-encrypt the entire secrets tree to the current recipient set.
6. **`gitvault recipient migrate`** — one-time migration from legacy flat file to named files.

Together they complete the git-native, no-shared-secret onboarding/offboarding loop.

## Recipient ceremony flow

### Onboarding a new team member

```
# New member — on their machine (single command)
gitvault identity create --add-recipient
# → creates key, stores in keyring
# → writes .secrets/recipients/alice.pub (git username auto-detected)
git commit -m "onboard: add alice as recipient" && git push   # open PR

# Maintainer — after PR merges
git pull
gitvault rekey                        # re-encrypts all .secrets/**/*.age to new recipient set
git add .secrets/
git commit -m "rekey: add alice"
git push

# New member — after maintainer push
git pull
gitvault decrypt .secrets/dev/app.env.age   # works immediately
```

### Offboarding a departing member

```
# Maintainer — surgical removal via named file
git rm .secrets/recipients/dave.pub
git commit -m "remove dave as recipient"
gitvault rekey                        # re-encrypts; dave's key no longer opens any file
git add .secrets/
git commit -m "rekey: remove dave"
git push
```

> **Note:** `rekey` mitigates *future access*. To close the historical window (Dave may have copied plaintext), rotate the credentials at the provider level as a separate step.

### Recipients directory layout

```
.secrets/
  recipients/
    alice.pub      ← added by alice via identity create --add-recipient
    bob.pub        ← added by bob
    ci-bot.pub     ← added by maintainer: gitvault recipient add <key> --name ci-bot
  dev/
    app.env.age
  prod/
    app.env.age
```

## Scope

| Command | New? | Notes |
|---|---|---|
| `gitvault identity pubkey` | ✅ new subaction | Extend existing `IdentityAction` enum |
| `gitvault identity create --add-recipient` | ✅ new flag | Extend existing `Create` variant |
| `gitvault recipient add-self` | ✅ new subaction | New `RecipientAction::AddSelf` variant |
| `gitvault recipient add --name <label>` | ✅ new flag | Extend existing `Add` variant |
| `gitvault recipient migrate` | ✅ new subaction | One-time flat-file → named-file migration |
| `gitvault rekey` | ✅ new top-level | New `src/commands/rekey.rs` + `Rekey` CLI variant |
| `gitvault recipient list` | updated | Reads from `recipients/` directory |
| `gitvault encrypt` / `decrypt` / `rekey` | updated | Scan `recipients/*.pub` instead of flat file |
| `gitvault harden` | existing (REQ-70) | Unchanged |
