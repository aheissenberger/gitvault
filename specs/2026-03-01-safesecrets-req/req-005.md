---
id: "S-20260301-R005"
title: "REQ-5 idempotent structured encryption"
status: "done"
owners: ["@aheissenberger"]
mode: ["cli"]
scope: { repoAreas: ["src/**"], touch: ["src/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "Implements REQ-5: structured encryption is idempotent; re-encrypting a field that is already age-armored preserves the existing ciphertext unchanged, ensuring minimal diffs across repeated runs."
verification: { commands: ["cargo test --all"] }
risk: { level: "medium" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-5

## Verification
- Idempotent ciphertext preservation is verified in src/structured/fields.rs (`test_json_field_encrypt_idempotent`, `test_json_only_changed_field_re_encrypts`) and src/structured/env_values.rs (`test_encrypt_env_values_is_idempotent_for_existing_ciphertext`).
- Stable structured serialization is verified by roundtrip and repeat-run tests for JSON/YAML/TOML handling in src/structured/fields.rs.

## Notes
- age encryption is non-deterministic by design (uses random nonces per encryption).
  The idempotent behaviour is achieved by detecting already-armored values and
  skipping re-encryption, NOT by producing the same ciphertext for the same plaintext.
