---
id: "S-20260302-R061"
title: "REQ-61 identity create command section"
status: "draft"
owners: ["@aheissenberger"]
mode: ["cli", "vscode-bg"]
scope: { repoAreas: ["src/**", "README.md", "specs/**"], touch: ["src/cli.rs", "src/dispatch.rs", "src/commands/**", "README.md", "specs/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "A new `identity` command section includes `identity create` with explicit `--profile classic|hybrid` selection."
  - id: "AC2"
    text: "`identity create` outputs machine-readable metadata in `--json` mode and never prints secret key material by default."
verification: { commands: ["cargo xtask spec-verify", "cargo test --all-targets --all-features"] }
risk: { level: "medium" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-61

## Verification
- CLI command surface tests for `identity create --profile classic|hybrid`
- JSON contract tests for metadata fields and secret-redaction defaults
