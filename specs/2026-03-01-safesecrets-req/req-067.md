---
id: "S-20260302-R067"
title: "REQ-67 Default adapter is built-in git hooks; external adapter name declared in .gitvault/config.toml"
status: "active"
owners: ["@aheissenberger"]
mode: ["cli", "vscode-ui", "vscode-bg"]
scope: { repoAreas: ["src/**", "specs/**", "README.md"], touch: ["src/repo/**", "src/commands/**", "src/cli.rs", "README.md", "specs/**"], avoid: ["target/**"] }
acceptance:
  - id: "AC1"
    text: "The default hook adapter is the built-in direct git-hooks mechanism (install_git_hooks). It activates unconditionally on harden when [hooks] adapter is absent from .gitvault/config.toml or the file does not exist."
  - id: "AC2"
    text: "An external hook-manager adapter (e.g. husky, pre-commit, lefthook) is activated only when .gitvault/config.toml contains [hooks] adapter = \"<name>\". The adapter binary invoked is 'gitvault-<name>' resolved from PATH."
  - id: "AC3"
    text: "When .gitvault/config.toml is absent, or [hooks] adapter is absent or empty, gitvault harden silently uses the built-in adapter with no error or warning."
  - id: "AC4"
    text: "When [hooks] adapter declares a name but the corresponding binary is absent from PATH, the behaviour follows the deterministic CI/--no-prompt contract defined in REQ-64..66 (fail with actionable message; warn in interactive mode)."
  - id: "AC5"
    text: ".gitvault/config.toml is the canonical TOML project-level config file for gitvault. Additional settings may be added to it in future requirements without breaking existing keys."
  - id: "AC6"
    text: "If REQ-68 user-global config exists (`~/.config/gitvault/config.toml`), repository `.gitvault/config.toml` remains authoritative for adapter selection when both define `[hooks] adapter`."
  - id: "AC7"
    text: "For adapter selection, empty string values are treated as unset in both repository and REQ-68 user-global config; unset resolves to built-in default behavior."
verification: { commands: ["cargo xtask spec-verify", "cargo test --all-targets --all-features"] }
risk: { level: "low" }
links: { issue: "", pr: "" }
---

## Requirement
- REQ-67

## Context
REQ-64, REQ-65 and REQ-66 introduce external hook-manager adapters for Husky, pre-commit and lefthook. The adapter selection must be declared explicitly by the repository owner using gitvault's own canonical config file rather than reading from hook-manager config files.

## Config File: `.gitvault/config.toml`

```toml
[hooks]
adapter = "husky"   # or "pre-commit" or "lefthook"
```

Supported `adapter` values: `husky`, `pre-commit`, `lefthook`. Any other non-empty value is a `Usage` error. Absent key or missing file → built-in default adapter.

The adapter binary discovered on PATH is `gitvault-<adapter>` (e.g. `gitvault-husky`).

If REQ-68 user-global config is present, this repository config remains canonical and overrides global fallback values for the same key.

Empty adapter values in either layer are treated as unset (equivalent to key absence), preserving built-in default adapter behavior.

## Verification
- Unit tests: `.gitvault/config.toml` absent → built-in adapter, no error.
- Unit tests: `[hooks] adapter = "pre-commit"` → activation attempted.
- Unit tests: `[hooks]` section absent → built-in adapter, no error.
- Unit tests: `adapter = "unknown"` → `GitvaultError::Usage`.
- Integration tests: `gitvault harden` without config file → only built-in hooks installed.

## Implementation-facing Acceptance Scenarios
- R067-S1: Given `.gitvault/config.toml` is absent, when `harden` runs, then only built-in git hooks are installed and no external-adapter logic runs.
- R067-S2: Given `.gitvault/config.toml` has `[hooks] adapter = "pre-commit"` and `gitvault-pre-commit` is on PATH, when `harden` runs, then the pre-commit adapter is invoked.
- R067-S3: Given `.gitvault/config.toml` exists but has no `[hooks]` section, when `harden` runs, then only built-in git hooks are installed.
- R067-S4: Given `[hooks] adapter = "lefthook"` but `gitvault-lefthook` is absent from PATH and `--no-prompt` is set, when `harden` runs, then the command exits non-zero with an actionable install message.
- R067-S5: Given `[hooks] adapter = "unknown-tool"`, when `harden` runs, then a `Usage` error is returned immediately.
- R067-S6: Given both repository and REQ-68 user-global config define `[hooks] adapter`, when `harden` runs, then repository config value is used.
- R067-S7: Given `[hooks] adapter = ""` in repository or user-global config and no higher-precedence value exists, when `harden` runs, then built-in hooks are selected with no error.
