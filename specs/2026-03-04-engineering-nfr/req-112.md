---
id: "S-20260304-R112"
title: "REQ-112: seal and unseal — In-Place Field-Level Encryption Commands"
status: "proposed"
owners: ["@aheissenberger"]
mode: ["cli"]
platforms: ["Linux", "macOS", "Windows"]
scope:
  repoAreas: ["src/**", "docs/**"]
  touch:
    - "src/cli.rs"
    - "src/dispatch.rs"
    - "src/commands/seal.rs"
    - "src/commands/unseal.rs"
    - "src/commands/mod.rs"
    - "src/structured/fields.rs"
    - "src/structured/env_values.rs"
    - "src/structured/mod.rs"
    - "src/config.rs"
    - "docs/reference.md"
    - "docs/secret-formats.md"
    - "README.md"
  avoid: ["target/**"]
breaking: true
supersedes: ["S-20260304-R111"]
acceptance:
  - id: "AC1"
    text: "`gitvault seal <FILE>` encrypts all eligible values in the file **in-place** (same path, same filename, file content transformed). Supported formats: `.json`, `.yaml`, `.yml`, `.toml`, `.env`, and any file whose name starts with `.env` (e.g. `.env.local`). The command MUST NOT write a `.age` file or touch `.gitvault/store/`."
  - id: "AC2"
    text: "For JSON/YAML/TOML: `seal` encrypts every string leaf value using age ASCII-armor (the existing `encrypt_armor` path). Non-string leaves (numbers, booleans, null, nested objects, arrays) are skipped silently. REQ-5 idempotency applies: already-encrypted string values are kept unchanged."
  - id: "AC3"
    text: "For `.env` files: `seal` encrypts every value using the `age:` prefix + binary base64 format (the existing `encrypt_env_values` path). Values already prefixed with `age:` are kept unchanged (idempotent)."
  - id: "AC4"
    text: "`seal` accepts an optional `--fields <FIELDS>` flag (comma-separated dot-path key names). When `--fields` is given, only those fields are encrypted; all other values are left unchanged. When `--fields` is omitted, all eligible string values are encrypted (auto-all is the default)."
  - id: "AC5"
    text: "`seal` accepts the standard global flags (`--identity`, `--identity-selector`, `--identity-stdin`, `--json`, `--no-prompt`) and a `-r / --recipient <PUBKEY>` flag (repeat for multi-recipient). When `--recipient` is omitted, recipients are resolved from `.gitvault/recipients/` as in `encrypt`."
  - id: "AC6"
    text: "`gitvault unseal <FILE> --reveal` decrypts all encrypted values found in the file, prints the result to stdout, and **never writes to disk**. This is the safe default and the primary intended usage."
  - id: "AC7"
    text: "`gitvault unseal <FILE>` (without `--reveal`) decrypts all encrypted values **in-place**, overwriting the file with plaintext. Before writing, the command (a) checks whether `<FILE>` is tracked by git (`git ls-files --error-unmatch`); if it is, it emits a stderr warning: `gitvault: warning: <FILE> is tracked by git. Commit only after re-sealing with 'gitvault seal <FILE>'.`; (b) always emits: `gitvault: warning: unseal without --reveal writes plaintext to disk. Ensure <FILE> is gitignored or commit only after re-sealing.` The command proceeds regardless; the warnings cannot be suppressed."
  - id: "AC8"
    text: "`unseal` accepts `--fields <FIELDS>`. When given, only the listed fields are decrypted; all other values (encrypted or plain) are left unchanged. When omitted, all encrypted values are decrypted (auto-discover using `collect_encrypted_field_paths`)."
  - id: "AC9"
    text: "`unseal` accepts `-i / --identity` and the standard global flags. It does not require `--env`; it operates entirely on the file at the given path."
  - id: "AC10"
    text: "When `seal` or `unseal` is called with an unsupported file extension, the command exits with a usage error: `seal/unseal requires a JSON, YAML, TOML, or .env file. For whole-file encryption use 'gitvault encrypt'.`"
  - id: "AC11"
    text: "`seal` registers the sealed file in `.gitvault/config.toml` under `[seal]` using a **glob pattern**. If `.gitvault/config.toml` does not exist, `seal` creates it with a minimal `[seal]` section. If the file's relative path (from repo root) is not already covered by any existing pattern in `[seal].patterns`, `seal` appends the most specific exact pattern that matches only that file (e.g. `\"config/app.json\"`). If `--fields` was given and the file is not already covered by a `[[seal.override]]` block, `seal` appends a new override block for that pattern. Existing entries are never removed or altered. Files under `.gitvault/` (including `.gitvault/config.toml` and `.gitvault/recipients/`) are **always implicitly excluded** from pattern matching and seal operations — `seal` must reject any file whose path starts with `.gitvault/` with error: `cannot seal gitvault configuration files`."
  - id: "AC12"
    text: "When `--fields` is given and the file IS already covered by an existing wildcard pattern (e.g. `\"config/**/*.json\"`), but has no matching `[[seal.override]]` block, `seal` appends a new `[[seal.override]]` block with `pattern = \"<exact-file-path>\"` and `fields = [<fields>]`. If a matching override already exists, it is updated to include any new fields (union, not replace). Fields already listed are left unchanged. All `config.toml` modifications MUST use `toml_edit` (comment-preserving TOML mutation) so that human-authored comments, formatting, and key ordering in the existing file are not destroyed."
  - id: "AC13"
    text: "`seal` performs its two writes (file in-place encryption + `config.toml` update) in order. If the `config.toml` update fails after the file was successfully sealed, `seal` exits non-zero and prints to stderr the exact TOML fragment the user must add manually: `gitvault: error: failed to update .gitvault/config.toml — add manually:\n  patterns = [\"<path>\"]`. The file encryption is NOT rolled back. This ensures the sealed file is always in a safe state even if the index update fails."
  - id: "AC13"
    text: "`gitvault status` reads `.gitvault/config.toml [seal]` patterns (and `[[seal.override]]` blocks), globs the working tree for matching files, then reads each matched file and verifies that at least one encrypted value exists. Files matching a pattern where zero encrypted values are found are reported as **drift** (unsealed plaintext). Files listed in a `[[seal.exclude]]` block are skipped. `status` never modifies any file."
  - id: "AC14"
    text: "`gitvault status` (read-only) reports two categories of drift side-by-side in one unified output: (1) **store drift** — `.age` files in `.gitvault/store/<env>/` whose source files have changed since last encrypt (existing REQ-32 behavior); (2) **seal drift** — files matched by `[seal].patterns` where one or more expected fields are not encrypted. For seal drift, when `[[seal.override]]` exists for a file, **all** listed fields must be encrypted (not just at least one). Files in `[[seal.exclude]]` are skipped. `status` exits with code 6 when `--fail-if-dirty` is given and any drift is found. `status` never modifies any file."
  - id: "AC15"
    text: "`gitvault status --fix` (mutating mode) applies automatic repairs: (a) **missing gitignore entries** — for every file path and directory that gitvault expects to be gitignored (`.git/gitvault/plain/`, materialized output dirs) but is not, appends the entry to `.gitignore`; (b) **missing pattern entries** — scans the working tree for files with at least one encrypted value (age armor or `age:` prefix) that are not covered by any pattern in `[seal].patterns`, and appends an exact-path entry for each. `--fix` reports each change made to stderr. It does NOT re-seal unsealed files, does NOT add gitignore entries for sealed source files (they are intentionally committed), and does NOT modify `[[seal.override]]` blocks."
  - id: "AC16"
    text: "`gitvault rekey` (spec-10, REQ-38) is extended: after re-encrypting `.age` files in `.gitvault/store/`, it additionally re-seals every file in the working tree matched by `[seal].patterns` (minus `[[seal.exclude]]`), using the `--fields` from the matching `[[seal.override]]` block when present. Re-seal uses the current recipients list. `--dry-run` reports what would be re-sealed without writing. `--env` applies only to the store-side re-encryption, not to sealed files (sealed files are environment-agnostic). Spec-10 must be updated to reference this extension."
  - id: "AC17"
    text: "The success message for `seal` reads: `Sealed N field(s) in <file>` (N = number of fields encrypted this run, 0 if all were already sealed). When `config.toml` was modified, an additional line reads: `Updated .gitvault/config.toml: added pattern \"<pattern>\"` (and/or `added override for \"<pattern>\"`). The success message for `unseal --reveal` reads: `Unsealed N field(s) from <file>` on stderr (stdout is the content)."
  - id: "AC18"
    text: "`src/config.rs` is extended with a `SealConfig` struct (and `SealOverride`, `SealExclude` sub-structs) added to `GitvaultConfig`. The intermediate raw structs (`RawSealConfig`, `RawSealOverride`, `RawSealExclude`) follow the existing `deny_unknown_fields` pattern. `SealConfig::default()` returns empty patterns (no sealing configured). The parser must accept `config.toml` files that have no `[seal]` section (backward compat). The `toml_edit` crate is used for mutation; the `toml` crate (serde-based) continues to be used for read-only parsing."
  - id: "AC19"
    text: "`gitvault harden` (spec-08, REQ-31) is extended: the pre-commit hook script it installs must additionally scan git-staged files that match `[seal].patterns` (as of the current `config.toml`) and block the commit if any such file contains zero encrypted values where at least one is expected (seal drift in the staged file). The hook uses `gitvault status --fail-if-dirty` or an equivalent internal check. This prevents accidentally committing an unsealed file. Spec-08 must be updated to reference this extension."
  - id: "AC20"
    text: "When `seal` is used with `--fields` on a `.env` file, `--fields` values are interpreted as **top-level keys** (no dot-path nesting, since `.env` files are flat). For example, `--fields DATABASE_URL,API_KEY` seals only those two keys. A dot-path like `--fields db.password` on a `.env` file exits with a usage error: `--fields dot-paths are not supported for .env files; use top-level key names (e.g. --fields DATABASE_URL).`"
  - id: "AC21"
    text: "All new public functions and command modules have rustdoc comments. Unit tests cover: seal JSON/YAML/TOML/.env roundtrip; seal idempotency; unseal --reveal; unseal in-place with git-tracked warning; --fields narrow selection; unsupported extension error; .gitvault/ path rejection; empty-file no-op; config.toml created from scratch when absent; config.toml pattern auto-add for uncovered file; config.toml override auto-add; override union merge; config.toml not modified when already covered; config.toml comments preserved after mutation; config.toml update failure prints manual fragment; status reports unified store + seal drift; status --fix adds missing gitignore entries; status --fix adds missing patterns; status --fix does not modify override blocks; .env --fields dot-path rejected; .gitvault/config.toml rejected as seal target."
verification:
  commands:
    - "cargo fmt --all -- --check"
    - "cargo clippy --all-targets -- -D warnings"
    - "cargo test --all"
risk:
  level: "medium"
links:
  related: "S-20260304-R110"
  supersedes: "S-20260304-R111"
  issue: ""
  pr: ""
---

# REQ-112: `seal` and `unseal` — In-Place Field-Level Encryption Commands

**ID:** S-20260304-R112
**Status:** Proposed
**Priority:** High
**Category:** Breaking Change / Developer Experience

## Problem

gitvault currently has two encryption models that silently diverge based on flags:

- `encrypt <file>` → writes to `.gitvault/store/<env>/<file>.age` (store model)
- `encrypt <file> --fields key` → modifies `<file>` in-place (in-place model)
- `encrypt <file> --value-only` → modifies `<file>` in-place (in-place model)

The same verb does fundamentally different things. A user who learns `encrypt` for whole files
is confused when `--fields` silently changes the output location. A user who learns
`--value-only` for `.env` cannot apply the same mental model to JSON.

**SOPS resolves this by having only one model** (always in-place). gitvault has two legitimate
models (whole-file archive for multi-env isolation; in-place for structured files). The solution
is to make each model a distinct top-level command with a distinct verb.

## Decision

Introduce `seal` and `unseal` as the canonical commands for **in-place field-level
encryption** of structured files. The existing `encrypt`/`decrypt` commands are reserved
exclusively for **whole-file archive encryption** (writing to/reading from `.gitvault/store/`).

| Verb | Model | Output location |
|---|---|---|
| `seal` | In-place, field-level | Same file path |
| `unseal` | In-place, field-level | Same file path (or stdout with `--reveal`) |
| `encrypt` | Whole-file archive | `.gitvault/store/<env>/<mirrored-path>.age` |
| `decrypt` | Whole-file archive | Original source path (resolved from store) |

## Command Specification

### `gitvault seal`

```
gitvault seal [OPTIONS] <FILE>
```

| Flag | Default | Description |
|---|---|---|
| `<FILE>` | — | JSON, YAML, TOML, or .env file to seal in-place |
| `--fields <FIELDS>` | all eligible values | Comma-separated dot-path keys to encrypt. Omit to encrypt all string values |
| `-r, --recipient <PUBKEY>` | `.gitvault/recipients/` | age public key (repeat for multi-recipient) |
| `-i, --identity <IDENTITY>` | — | Identity key file (also `GITVAULT_IDENTITY`) |
| `-e, --env <ENV>` | — | Environment (used for recipient resolution only) |

**File format dispatch:**

| Extension | Mechanism | Idempotency check |
|---|---|---|
| `.json` | age ASCII-armor per string leaf | `is_age_armor(value)` |
| `.yaml` / `.yml` | age ASCII-armor per string leaf | `is_age_armor(value)` |
| `.toml` | age ASCII-armor per string leaf | `is_age_armor(value)` |
| `.env` / `.env.*` | `age:` prefix + binary base64 | `is_env_encrypted(value)` |

### `gitvault unseal`

```
gitvault unseal [OPTIONS] <FILE>
```

| Flag | Default | Description |
|---|---|---|
| `<FILE>` | — | Sealed file to unseal |
| `--reveal` | off | **Recommended.** Print plaintext to stdout; never write to disk |
| `--fields <FIELDS>` | all encrypted values | Comma-separated dot-path keys to decrypt |
| `-i, --identity <IDENTITY>` | — | Identity key file (also `GITVAULT_IDENTITY`) |

## `.gitvault/config.toml` — seal index (Option C, pattern-based)

Rather than a separate `sealed.toml` state file, the index of sealed files is declared as
**intent patterns** in the existing `.gitvault/config.toml`. This eliminates merge conflicts:
two branches that each seal a different file produce two independent `[[seal.override]]` blocks
or two entries appended to `patterns` — both merge cleanly as they do not touch the same TOML
key.

### Schema

```toml
# .gitvault/config.toml

[seal]
# Files matching these glob patterns are expected to have all eligible string values sealed.
# gitvault seal auto-appends here when a sealed file is not already covered.
patterns = [
  "**/.env*",           # all .env variants anywhere in the repo
  "config/**/*.json",   # all JSON under config/
  "helm/values.yaml",   # exact path
]

# Per-pattern field overrides: only the listed fields are expected to be sealed.
# When --fields is used on a file covered by a wildcard pattern, seal auto-appends here.
[[seal.override]]
pattern = "helm/values.yaml"
fields  = ["postgresql.auth.password", "redis.auth.password"]

# Files matching these patterns are never flagged by status even if unsealed.
[[seal.exclude]]
pattern = "config/public.json"
```

### Auto-update rules (enforced by `seal`)

| Situation | Action taken by `seal` |
|---|---|
| `config.toml` absent | Create file with minimal `[seal]\npatterns = ["<path>"]` |
| File not covered by any `patterns` entry, no `--fields` | Append `"<relative-path>"` to `patterns` |
| File not covered by any `patterns` entry, with `--fields` | Append `"<relative-path>"` to `patterns` **and** append `[[seal.override]]` block |
| File covered by a wildcard pattern, with `--fields`, no override exists | Append `[[seal.override]]` with exact path and fields |
| File covered by a wildcard pattern, with `--fields`, override exists | Union-merge new fields into existing override's `fields` array |
| File covered exactly (or by wildcard), no `--fields`, no override | No config change (file is already fully covered) |
| File covered by `[[seal.exclude]]` | Warn to stderr and proceed; do not modify config |
| File path starts with `.gitvault/` | Exit with error: `cannot seal gitvault configuration files` |

All config mutations use `toml_edit` to preserve comments and formatting in the existing file.

### Drift detection (`gitvault status` and `status --fix`)

`status` (read-only) reports two categories unified in one output:

**Category 1 — Store drift** (existing REQ-32): `.age` files in `.gitvault/store/<env>/` whose source has changed.

**Category 2 — Seal drift** (new): evaluates each pattern in `[seal].patterns`:

1. Glob the working tree for matching files (respecting `[[seal.exclude]]` and always skipping `.gitvault/**`).
2. For each matched file, read it and identify encrypted values.
3. If a `[[seal.override]]` exists for this file, verify that **each** listed field is encrypted (not just one — all must be sealed).
4. If no override exists, verify at least one encrypted value exists anywhere in the file.
5. Drift = any expected field is found to be plaintext.

```
gitvault status

Store:
  ✓  prod/services/auth/config.json.age    (up to date)
  ✗  prod/.env.age                         drift: source modified

Sealed:
  ✓  helm/values.yaml                      (2/2 fields sealed)
  ✓  config/app.json                       (all fields sealed)
  ✗  .env                                  drift: 0 of 3 values encrypted — run 'gitvault seal .env'
     config/public.json                    excluded
```

Exit code 6 when `--fail-if-dirty` is set and any drift is found (store or seal).

**`status --fix`** applies automatic repairs without re-sealing or re-encrypting:

| Repair | Action |
|---|---|
| Missing `.gitignore` entry for gitvault output dirs | Appends entry to `.gitignore` |
| File with encrypted values not covered by `[seal].patterns` | Appends exact-path entry to `patterns` |
| Override union / drift re-seal | **Not performed** — `--fix` is non-destructive to file content |

`--fix` reports each change on stderr. It never modifies file content, never adds `[[seal.override]]` blocks, and never removes patterns.

### Why not `sealed.toml`?

| Property | `sealed.toml` (state file) | `config.toml [seal]` (intent patterns) |
|---|---|---|
| Merge conflicts | Yes — two branches both write to same keys | No — pattern list is append-only |
| New files auto-covered | No — must run `seal` and commit index | Yes — wildcard patterns cover future files |
| Drift detection | State vs. disk comparison | Intent vs. disk comparison |
| Familiar mental model | Lockfile | `.gitignore` / `.gitattributes` |
| Requires `seal` to update index | Always | Only when file isn't already covered |
| Comment preservation | N/A | Required — use `toml_edit` for mutations |

## `src/config.rs` Schema Extension

The `GitvaultConfig` struct gains a `seal: SealConfig` field. Intermediate raw structs follow the existing `deny_unknown_fields` pattern so unknown `[seal]` sub-keys produce a usage error immediately:

```rust
pub struct GitvaultConfig {
    pub hooks: HooksConfig,
    pub env: EnvConfig,
    // ... existing fields ...
    pub seal: SealConfig,    // new
}

pub struct SealConfig {
    pub patterns: Vec<String>,
    pub overrides: Vec<SealOverride>,
    pub excludes: Vec<SealExclude>,
}

pub struct SealOverride {
    pub pattern: String,
    pub fields: Vec<String>,
}

pub struct SealExclude {
    pub pattern: String,
}
```

`SealConfig::default()` returns empty vectors (no patterns = no sealing configured). Absence of `[seal]` in `config.toml` is not an error. The `toml_edit` crate handles all writes; the `toml` + serde path continues for reads.

## Relationship to Previous Requirements

| Requirement | Disposition |
|---|---|
| REQ-4 (`encrypt --fields`) | **Superseded** — use `seal --fields` |
| REQ-6 (`encrypt --value-only`) | **Superseded** — use `seal` on `.env` |
| REQ-31 (`harden` hook install) | **Extended** — hook must also check staged sealed-pattern files (AC19) |
| REQ-32 (`status --fail-if-dirty`) | **Extended** — unified store + seal drift output (AC14) |
| REQ-38 (`rekey`) | **Extended** — also re-seals pattern-matched files (AC16) |
| REQ-67 (`config.toml`) | **Extended** — gains `[seal]` section; `src/config.rs` gains `SealConfig` (AC18) |
| REQ-110 (decrypt auto-discover) | Separate supersession via REQ-114 |
| REQ-111 (`--all-fields` on encrypt) | **Superseded before implementation** — use `seal` (all-by-default) |

## Migration Path

```bash
# Before → After

gitvault encrypt app.json --fields db.pass --env dev
→ gitvault seal app.json --fields db.pass

gitvault encrypt .env --value-only --env dev
→ gitvault seal .env

gitvault decrypt .gitvault/store/dev/app.json.age --fields db.pass --reveal
→ gitvault unseal app.json --fields db.pass --reveal

gitvault decrypt .gitvault/store/dev/.env.age --value-only
→ gitvault unseal .env
```

## Test Plan

- Roundtrip: `seal` then `unseal --reveal` returns original content for JSON/YAML/TOML/.env.
- Idempotency: `seal` on already-sealed file produces identical ciphertext (REQ-5).
- `--fields` narrow: only listed fields change; others untouched.
- `unseal` in-place: plaintext written to file; both warnings emitted to stderr.
- `unseal` in-place on git-tracked file: git-tracked warning emitted.
- `unseal --reveal`: stdout contains plaintext; file on disk unchanged.
- Unsupported extension: usage error with guidance.
- `.gitvault/config.toml` as seal target: rejected with error.
- `.env --fields` with dot-path: rejected with usage error; top-level key works.
- **config.toml created from scratch:** `seal` on repo with no `config.toml` creates the file.
- **config.toml auto-update — exact path:** appended to `patterns`; subsequent `seal` does not re-append.
- **config.toml auto-update — with --fields:** appends pattern + `[[seal.override]]` block.
- **config.toml auto-update — wildcard covered, --fields:** appends only override block.
- **config.toml auto-update — override union merge:** existing `["api.key"]` + `--fields db.pass` → `["api.key", "db.pass"]`.
- **config.toml not modified:** already fully covered file → byte-identical config after seal.
- **config.toml comment preservation:** comments in existing `config.toml` survive mutation.
- **config.toml update failure:** manual fragment printed to stderr; sealed file not rolled back.
- **status unified output:** store drift + seal drift reported together.
- **status drift precision:** `[[seal.override]]` with 2 fields, 1 plaintext → reported as drift.
- **status clean:** after `seal`, all pattern-matched files reported as sealed.
- **status exclude:** `[[seal.exclude]]` file not reported as drift.
- **status --fix gitignore:** missing output dir entry added to `.gitignore`.
- **status --fix pattern:** file with encrypted values but no pattern entry gets exact-path added.
- **status --fix non-destructive:** override blocks untouched; file content untouched.
- `rekey` re-seals all pattern-matched files with new recipients; respects `[[seal.override]]` fields.
