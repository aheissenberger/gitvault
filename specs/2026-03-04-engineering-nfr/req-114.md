---
id: "S-20260304-R114"
title: "REQ-114: Breaking Changes to decrypt — Source-Path Resolution, Remove --fields / --value-only"
status: "draft"
owners: ["@aheissenberger"]
mode: ["cli"]
platforms: ["Linux", "macOS", "Windows"]
scope:
  repoAreas: ["src/**", "docs/**"]
  touch:
    - "src/lib.rs"
    - "src/cli.rs"
    - "src/dispatch.rs"
    - "src/commands/decrypt.rs"
    - "src/store.rs"
    - "docs/reference.md"
    - "docs/secret-formats.md"
    - "README.md"
  avoid: ["target/**"]
breaking: true
supersedes: ["S-20260301-R005", "S-20260301-R007", "S-20260304-R110"]
acceptance:
  - id: "AC1"
    text: "`gitvault decrypt <SOURCE-FILE>` accepts the **original source path** (e.g. `services/auth/config.json`) and automatically resolves the corresponding store path: `.gitvault/store/<env>/services/auth/config.json.age`. The `--env` flag selects the environment; `GITVAULT_ENV` is the fallback."
  - id: "AC2"
    text: "`gitvault decrypt <STORE-PATH>` still accepts an explicit `.age` store path for backward compatibility and scripting. Before applying the two-part check, any absolute path is converted to a repo-relative path by stripping the repo root prefix (lexically, using `Path::strip_prefix(repo_root)`). **`std::fs::canonicalize` is NOT used** for the repo-root stripping or the two-part check — all path operations in this step are purely lexical (consistent with `compute_store_path` in REQ-113 AC5). A path is treated as an explicit store path when it: (a) has the `.age` extension **and** (b) the repo-relative path begins with `.gitvault/store/`. All other inputs trigger source-path resolution (AC1). This two-part check prevents ambiguous filenames like `data.age.bak` or a source file accidentally named `something.age` from being silently misrouted."
  - id: "AC3"
    text: "`--fields <FIELDS>` is removed from `gitvault decrypt`. Passing it produces a usage error: `--fields is no longer supported on decrypt. Use 'gitvault unseal --fields <FIELDS> <FILE>' for in-place field-level decryption.`"
  - id: "AC4"
    text: "`--value-only` is removed from `gitvault decrypt`. Passing it produces a usage error: `--value-only is no longer supported. Use 'gitvault unseal <FILE>' to decrypt .env values in-place, or 'gitvault unseal --reveal <FILE>' to print to stdout.`"
  - id: "AC5"
    text: "When source-path resolution is applied and no matching `.age` file is found in the store for the given env, the command exits with a clear error: `No encrypted archive found for '<FILE>' in environment '<ENV>'. Run 'gitvault encrypt <FILE> --env <ENV>' to create it.`"
  - id: "AC6"
    text: "`gitvault decrypt` retains `--reveal` (print decrypted content to stdout instead of materialising to disk). `--output/-o` is **removed** (see AC10). When materialising (i.e. `--reveal` is NOT given), the decrypted output is written to `<PLAIN_BASE_DIR>/<env>/<relative-source-path>`, mirroring the source path structure (e.g. `services/auth/config.json` → `.git/gitvault/plain/prod/services/auth/config.json`). This prevents filename collisions between files with the same name in different directories. For backward-compat explicit `.age` paths (AC2), the relative source path is derived by stripping `.gitvault/store/<env>/` from the store path and removing the `.age` suffix; the `<env>` used in the output path is always the env segment parsed from the store path itself, NOT the `--env` flag or `GITVAULT_ENV` (which are irrelevant once an explicit `.age` path is provided). The env segment is derived by stripping the repo-relative prefix `.gitvault/store/` from the store path and taking the **first remaining path component** (i.e., the immediate subdirectory of `.gitvault/store/`). When an explicit `.age` store path is detected and `--env` was also supplied on the command line, the command emits a stderr warning: `gitvault: warning: --env <X> is ignored when an explicit .age store path is given; using env '<parsed-env>' from the store path.` The command then proceeds normally. All path operations for output-path derivation are purely lexical (`std::fs::canonicalize` is NOT used — consistent with REQ-113 AC5)."
  - id: "AC7"
    text: "The REQ-110 auto-discover branch (decrypt non-`.age` structured files by collecting encrypted field paths) is **removed** from `src/commands/decrypt.rs`. This functionality is entirely replaced by `unseal` (REQ-112)."
  - id: "AC8"
    text: "The `src/commands/decrypt.rs` `cmd_decrypt` function signature no longer accepts `fields: Option<Vec<String>>` or `value_only: bool`. `src/lib.rs` already gains `pub mod store;` via REQ-113 AC7. The decrypt side introduces a `resolve_store_path(input: &Path, env: &str, repo_root: &Path) -> Result<PathBuf, GitvaultError>` helper in `src/commands/decrypt.rs` (or `src/store.rs`) that: (1) applies the two-part check (AC2) — if the input is an explicit `.age` store path, returns it directly; (2) otherwise calls `store::compute_store_path(input, env, repo_root)` to derive the mirrored path; (3) verifies the derived path exists on disk, returning `GitvaultError::NotFound` (wrapping AC5's message) if absent. The path-formula logic MUST NOT be duplicated — step (2) always delegates to `store::compute_store_path`."
  - id: "AC9"
    text: "The CLI `--help` output for `decrypt` reads: `Decrypt a file from the .gitvault/store/<env>/ archive. Accepts either the original source path or the .age store path. For in-place field-level decryption of JSON/YAML/TOML/.env, use 'gitvault unseal'.`"
  - id: "AC10"
    text: "All existing unit and integration tests for `decrypt` that reference `--fields`, `--value-only`, or `--output` / the current adjacent-file output path (stripping `.age` suffix and writing next to input) are updated or removed. `src/cli.rs` `Decrypt` variant: remove the `output` field (`-o, --output`). `src/dispatch.rs`: remove the `output` argument from the `cmd_decrypt` call. `src/commands/decrypt.rs`: remove `resolve_output_path` and `OUTPUT_KEEP_PATH_SENTINEL`. A new test asserts source-path resolution: given a source path with subdirectories, `compute_store_path` (from `src/store.rs`) returns the correct mirrored `.age` store path. A second test asserts that an absolute `.age` store path under the repo root is correctly recognised as an explicit store path after repo-root stripping. A third test asserts that materialisation writes to `<PLAIN_BASE_DIR>/<env>/<relative-source-path>` (not adjacent to input)."
verification:
  commands:
    - "cargo fmt --all -- --check"
    - "cargo clippy --all-targets -- -D warnings"
    - "cargo test --all"
    - "! grep -rn '\\-\\-fields\\|value_only\\|--value-only' src/commands/decrypt.rs src/cli.rs"
risk:
  level: "high"
links:
  related: "S-20260304-R112"
  related2: "S-20260304-R113"
  supersedes: "S-20260304-R110"
  issue: ""
  pr: ""
---

# REQ-114: Breaking Changes to `decrypt`

**ID:** S-20260304-R114
**Status:** Proposed
**Priority:** High
**Category:** Breaking Change / DX Coherence

## Problem

`gitvault decrypt` currently requires the caller to know and provide the `.age` store path:

```bash
gitvault decrypt .gitvault/store/prod/config.json.age --reveal
```

This is an implementation detail that leaks the store's directory structure into the user's
workflow. It also means that scripts must reconstruct the store path manually, and users who
move files (changing the source path) must remember to update their decrypt commands.

Additionally, `decrypt` accepts `--fields` and `--value-only` which were already removed from
`encrypt` (REQ-113) and superseded by `seal`/`unseal` (REQ-112). Their presence on `decrypt`
creates a mismatch and must be removed for consistency.

## Breaking Changes

### 1. Source-path resolution — accept original path

Users now pass the **source file path**, not the store path:

```bash
# Before
gitvault decrypt .gitvault/store/prod/services/auth/config.json.age

# After
gitvault decrypt services/auth/config.json --env prod
```

The source path is resolved to the store path mechanically:
```
<source-path>  →  .gitvault/store/<env>/<source-path>.age
```

**Backward compatibility:** `.age` paths are still accepted via the two-part check in AC2: when `<FILE>` has the `.age` extension **and** begins with `.gitvault/store/`, it is used directly as the store path (no resolution). Files that merely end with `.age` but are not under `.gitvault/store/` (e.g. `backup/config.json.age`) are treated as source paths and trigger resolution. This allows existing CI scripts to continue working during a migration period.

```bash
# Still works (explicit .age path)
gitvault decrypt .gitvault/store/prod/services/auth/config.json.age
```

### 2. Remove `--fields`

`decrypt --fields` is superseded by `unseal --fields` (REQ-112):

```bash
# Before
gitvault decrypt .gitvault/store/prod/config.json.age --fields db.password --reveal

# After
gitvault unseal config.json --fields db.password --reveal
```

### 3. Remove `--value-only`

`decrypt --value-only` is superseded by `unseal` on `.env` files:

```bash
# Before
gitvault decrypt .gitvault/store/dev/.env.age --value-only

# After
gitvault unseal .env
```

### 4. Remove REQ-110 auto-discover branch

The auto-discover branch added by REQ-110 (detecting non-`.age` JSON/YAML/TOML files and
auto-decrypting encrypted fields) is removed from `cmd_decrypt`. Its functionality is now
provided cleanly by `unseal`:

```bash
# REQ-110 auto-discover (removed)
gitvault decrypt config.json --reveal

# Replacement (REQ-112)
gitvault unseal config.json --reveal
```

The removal avoids ambiguity: `decrypt` always means "fetch from store". `unseal` always
means "decrypt fields in-place or reveal to stdout".

## `decrypt` Command After Changes

```
gitvault decrypt [OPTIONS] <FILE>
```

| Flag | Default | Description |
|---|---|---|
| `<FILE>` | — | Original source path (or explicit `.age` store path) |
| `--reveal` | off | Print decrypted content to stdout; do not write to `.git/gitvault/plain/` |
| `-e, --env <ENV>` | — | Environment for store path resolution (overrides `GITVAULT_ENV`) |
| `-i, --identity <IDENTITY>` | — | Identity key file |

**Removed flags:** `--fields`, `--value-only`

**Source-path resolution algorithm:**

```
if FILE has extension ".age" AND FILE starts with ".gitvault/store/":
    use FILE directly as store path
else:
    store_path = <repo-root>/.gitvault/store/<env>/<FILE>.age
    if store_path does not exist:
        error: "No encrypted archive found for '<FILE>' in environment '<ENV>'. Run 'gitvault encrypt <FILE> --env <ENV>' to create it."
```

## Implementation Notes

- `compute_store_path(source: &Path, env: &str, repo_root: &Path) -> Result<PathBuf, GitvaultError>`
  is the **single shared function** in `src/store.rs` used by both `encrypt` (REQ-113) and `decrypt`
  (REQ-114). It computes `<repo_root>/.gitvault/store/<env>/<source_relative>.age`. Both commands
  import it from `src/store.rs`; the logic is not duplicated.
- Remove `auto_discover` branch (added in REQ-110, lines ~138–180 in current `decrypt.rs`).
- Remove `--fields` and `--value-only` arms from the match in `cmd_decrypt`.
- Update CLI struct in `cli.rs`: remove `fields`, `value_only` from `Decrypt` variant.
- Update `dispatch.rs`: remove corresponding arguments from `cmd_decrypt` call.
- The `structured` module's `collect_encrypted_field_paths` and `decrypt_fields_content`
  remain in the codebase (used by `unseal`); they are no longer called from `cmd_decrypt`.
