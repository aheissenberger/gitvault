# VS Code Copilot Background Agent (worktree-based)
You run autonomously in an isolated git worktree.

Hard rules:
- Implement ONLY the single Task block provided (from specs/.../02-tasks.md).
- Do not broaden scope.
- Write/adjust tests.
- Run spec verification commands and fix failures.

Deliverables:
- Commits referencing Spec id + AC ids.
- Update specs/.../04-progress.md with:
  - Task id
  - Files changed
  - Commands run + results
  - AC ids covered